name: Railway Deploy 3
on:
  push:
    branches:
      - dev
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: bervProject/railway-deploy@0.0.1-alpha
        with:
          railway_token: ${{ secrets.RAILWAY_TOKEN }}
